## Overview

This repository includes simple JavaScript examples to practice loops and array methods. It demonstrates the use of `for`, `forEach`, `for...of`, and `for...in` loops, along with `map`, `filter`, and `reduce`. These programs help understand how to iterate through arrays and objects, modify values, filter data, and calculate totals
